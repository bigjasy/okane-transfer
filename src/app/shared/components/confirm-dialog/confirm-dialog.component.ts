import { Component, EventEmitter, Input, Output } from '@angular/core';
@Component({ selector: 'app-confirm-dialog', standalone: true, template: `@if(open){<div class="overlay"><div class="card dialog"><h3>{{title}}</h3><p>{{message}}</p><div><button class="btn" (click)="cancel.emit()">Annuler</button><button class="btn danger" (click)="confirm.emit()">Confirmer</button></div></div></div>}`, styles: [`.overlay{position:fixed;inset:0;background:rgba(15,23,42,.45);display:grid;place-items:center;z-index:10}.dialog{max-width:420px}.dialog div{display:flex;justify-content:flex-end;gap:.7rem}`] })
export class ConfirmDialogComponent { @Input() open=false; @Input() title='Confirmation'; @Input() message='Confirmer cette action ?'; @Output() confirm = new EventEmitter<void>(); @Output() cancel = new EventEmitter<void>(); }
