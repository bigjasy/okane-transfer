import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { StatusBadgeComponent } from '../../../shared/components/status-badge/status-badge.component';
import { DataService } from '../../../core/services/data.service';
import { CASH_MOVEMENT_TYPES } from '../../../core/models/enums';
@Component({selector:'app-cash-register',standalone:true,imports:[FormsModule,StatusBadgeComponent],template:`<section class="page"><h1 class="page-title">Caisse agent</h1>@if(cash){<div class="card"><h3>Caisse courante</h3><p>{{cash.agentName}} - {{cash.currentBalance}} {{cash.currencyCode}}</p><app-status-badge [value]="cash.status"/></div>}<div class="grid cols-3"><div class="card"><h3>Ouvrir</h3><input type="number" [(ngModel)]="open.agentId" name="a"><input type="number" [(ngModel)]="open.agencyId" name="ag"><input [(ngModel)]="open.currencyCode" name="cur"><input type="number" [(ngModel)]="open.openingBalance" name="bal"><button class="btn primary" (click)="openCash()">Ouvrir</button></div><div class="card"><h3>Mouvement</h3><select [(ngModel)]="movement.type" name="mt">@for(t of types; track t){<option [value]="t">{{t}}</option>}</select><input type="number" [(ngModel)]="movement.amount" name="ma"><input [(ngModel)]="movement.reason" name="mr"><button class="btn" (click)="addMovement()">Ajouter</button></div><div class="card"><h3>Clôturer</h3><input type="number" [(ngModel)]="closing.countedAmount" name="ca"><input [(ngModel)]="closing.comment" name="cc"><button class="btn danger" (click)="closeCash()">Clôturer</button></div></div><div class="card table-wrap"><table><tr><th>Type</th><th>Montant</th><th>Raison</th><th>Date</th></tr>@for(m of movements; track m.id){<tr><td>{{m.type}}</td><td>{{m.amount}} {{m.currencyCode}}</td><td>{{m.reason}}</td><td>{{m.createdAt}}</td></tr>}</table></div></section>`})
export class CashRegisterComponent implements OnInit{types=CASH_MOVEMENT_TYPES;cash:any;movements:any[]=[];open={agentId:3,agencyId:1,currencyCode:'MAD',openingBalance:10000};movement={type:'ADJUSTMENT' as const,amount:100,currencyCode:'MAD',reason:'Correction'};closing={countedAmount:10000,comment:'Clôture'};constructor(private data:DataService){}ngOnInit(){this.load()}load(){this.data.currentCash().subscribe(x=>this.cash=x);this.data.movements().subscribe(x=>this.movements=x)}openCash(){this.data.openCash(this.open).subscribe(()=>this.load())}addMovement(){this.data.addMovement(this.cash?.id||1,this.movement).subscribe(()=>this.load())}closeCash(){this.data.closeCash(this.cash?.id||1,this.closing).subscribe(()=>this.load())}}
