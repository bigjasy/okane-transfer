import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { DataService } from '../../../core/services/data.service';
import { IDENTITY_TYPES } from '../../../core/models/enums';
@Component({selector:'app-payout',standalone:true,imports:[FormsModule],template:`<section class="page"><h1 class="page-title">Paiement / retrait</h1><div class="grid cols-3"><div class="card"><h3>1. Recherche</h3><input [(ngModel)]="withdrawalCode" name="w" placeholder="Code retrait"><input [(ngModel)]="phone" name="p" placeholder="Téléphone bénéficiaire"><button class="btn primary" (click)="search()">Rechercher</button></div><div class="card"><h3>2. Identité</h3><input [(ngModel)]="transferReference" name="ref" placeholder="Référence"><select [(ngModel)]="identityType" name="idt">@for(i of identityTypes; track i){<option [value]="i">{{i}}</option>}</select><input [(ngModel)]="identityNumber" name="idn" placeholder="Numéro identité"><button class="btn" (click)="validate()">Valider</button></div><div class="card"><h3>3. OTP</h3><input [(ngModel)]="otpCode" name="otp" placeholder="OTP"><button class="btn success" (click)="confirm()">Payer</button></div></div>@if(result){<div class="ok-box">Paiement confirmé: {{result.transferReference}} - {{result.paidAmount}} {{result.currency}} - {{result.paidAt}}</div>}</section>`})
export class PayoutComponent{identityTypes=IDENTITY_TYPES;withdrawalCode='123456';phone='+33612345678';transferReference='OKN-2026-0001';identityType='PASSPORT' as const;identityNumber='P123456';otpCode='123456';result:any;constructor(private data:DataService){}search(){this.data.searchPayout().subscribe(t=>this.transferReference=t.reference)}validate(){this.data.validatePayout().subscribe()}confirm(){this.data.confirmPayout({transferReference:this.transferReference,withdrawalCode:this.withdrawalCode,identityType:this.identityType,identityNumber:this.identityNumber,otpCode:this.otpCode}).subscribe(x=>this.result=x)}}
