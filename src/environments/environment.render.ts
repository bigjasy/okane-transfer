// Replaced at build time by scripts/render-set-api-url.js (see render.yaml).
export const environment = {
  production: true,
  apiBaseUrl: '__API_BASE_URL__',
  useMockApi: false,
  allowMockFallback: false
};
